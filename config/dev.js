const config = {
  domain: "local.dev:3000",
  protocol: "http",
  db: {
    database: 'pnyka',
    username: 'postgres',
    password: 1234,
    host: 'localhost',
    port: 5432,
  }
}

module.exports = config;